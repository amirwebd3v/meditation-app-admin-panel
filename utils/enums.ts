enum CourseType {
    Meditation = 'MEDITATION',
    Video = 'VIDEO',
}

enum CourseSet {
    Single = 'SINGLE',
    Course = 'MULTIPLE',
}

enum MediaType {
    TRACK = 'track',
    PICTURE = 'picture',
}

export {
    CourseType, CourseSet, MediaType
}